#include<iostream>
using namespace std;
int main()
{
   char ch[100];
   cin>>ch;//Input: apple     , Input:apple is healthy
   cout<<ch; //Output:apple   , Output:apple
}//it means cin only take input untile it does not encounter a space character for the prog to take whole
//sentence as an input use cin.getline(arr,n) where arr is name of the array and n is the no. of elements in the array 